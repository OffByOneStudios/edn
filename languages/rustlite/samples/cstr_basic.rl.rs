// cstr_basic.rl.rs
// Restored literal convenience macros (rcstr / rbytes) expand to core cstr/bytes ops.
// Example macro IR (not Rust syntax): (rcstr %s "hello"), (rbytes %d [ 1 2 3 ])
// This sample remains a placeholder until a more Rust-like surface syntax is added.

fn main() { }
